# Production dependencies (installed on Render).
# Pinned exact versions for reproducible builds; regenerate from a known-good
# env with: pip freeze | grep -Ei 'fastapi|uvicorn|pandas|multipart|pydantic|scikit|sqlalchemy|psycopg2'
fastapi==0.141.1
uvicorn[standard]==0.53.0
pandas==3.0.5
python-multipart==0.0.32
pydantic==2.13.5
scikit-learn==1.9.1
sqlalchemy==2.0.54
psycopg2-binary==2.9.13
